apiVersion: 1

providers:
  - name: "aqara2tig" # A uniquely identifiable name for the provider
    folder: "Demo dashboards" # The folder where to place the dashboards
    type: file
    options:
      path: "/etc/grafana/provisioning/dashboards"
